<!-- <p id="notice"><%= notice %></p>

<p>
<b>Fname:</b>
<%= @tester.fname %>
</p>

<p>
<b>Lname:</b>
<%= @tester.lname %>
</p>

<p>
<b>Email:</b>
<%= @tester.email %>
</p>

<p>
<b>Gender:</b>
<%= @tester.gender %>
</p>

<p>
<b>Phone:</b>
<%= @tester.phone %>
</p>

<p>
<b>City:</b>
<%= @tester.city %>
</p>

<p>
<b>Country:</b>
<%= @tester.country %>
</p>

<p>
<b>Twitterid:</b>
<%= @tester.twitterid %>
</p>

<p>
<b>Linkedinid:</b>
<%= @tester.linkedinid %>
</p>

<p>
<b>Paypalid:</b>
<%= @tester.paypalid %>
</p>

<p>
<b>Bankaccount:</b>
<%= @tester.bankaccount %>
</p>

<p>
<b>Aboutme:</b>
<%= @tester.aboutme %>
</p>


<%= link_to 'Edit', edit_tester_path(@tester) %> |
<%= link_to 'Back', testers_path %> -->

<% provide(:title, @tester.fname) %>

<div class="clearfix wide-box page-description">
  <div class="container">
    <h2>Do you have a product or service you want to have tested?</h2>
    <div class="comment">We call these testing projects Challenges. This means you challenge testers to give their best to make your product better. Sounds good? We can't wait to see what new and exciting challenges you set up for our testers.</div>
  </div>
</div>


<div class="row">
  <div class="span9">
    <!-- In progress tests -->
    <section id="inprogress" class="client-version-box">
      <div class="client-version-section">
        <!-- earn-title -->
        <div class="client-version-title">
          <div class="title">
            <h2>ALL YOUR CHALLENGES</h2>
          </div>
        </div>

        <div class="media">
          <a href="#" class="pull-left"><img src="http://lorempixel.com/64/64/" class="media-object" alt='' /></a>
          <div class="media-body">
            <h4 class="media-heading">
              Nested media heading
            </h4> Cras sit amet nibh libero, in gravida nulla. Nulla vel metus scelerisque ante sollicitudin commodo. Cras purus odio, vestibulum in vulputate at, tempus viverra turpis.
          </div>
        </div>

        <!-- table-box -->
        <div class="table-box">
          <table class="table table-bordered">
            <thead>

            <tr>
              <th class="col1">Project name</th>
              <!-- <th class="col2">Question</th>  -->
              <th class="col2">Test Start</th>
              <th class="col3">Test End</th>
              <th class="clo4">URL</th>
              <th class="clo5"># of Available Testers</th>
              <th class="clo6"># of issues</th>
              <th class="clo7">Assigned</th>
              <th class="clo8"></th>
            </tr>

            </thead>
            <tbody>
            <% if @checknbofproject.count != 2 %>

                <% @feedback.each do |feedback| %>

                    <tr>
                      <td class="col1"><%= link_to feedback.name, "/feedbacks/#{feedback.id}" %></td>
                      <!-- <td class="col2"><%#= feedback.questions %></td>  -->
                      <td class="col2"><%= feedback.updated_at %></td>
                      <td class="col3"><%= feedback.duedate %></a></td>
                      <td class="clo4"><%= feedback.url %></td>
                      <td class="clo5"><%= feedback.avai_tester %></td>
                      <td class="clo6"><%= feedback.issues.count %></td>
                      <% if feedback.assignments.where('tester_id = ?', @tester.id).empty? %>
                          <td class="clo7"><%= link_to 'Assigned ->', {:action => :assigned, :assigned => feedback.id}, :method => :put  %></td>
                          <td class="clo8">You need to apply for this project first</td>
                      <% else %>
                          <td class="clo7">You are already Assigned</td>
                          <td class="clo8"><%= link_to 'New Issue ->', new_issue_path(:feedback_id => feedback.id) %></td>
                      <% end %>
                    </tr>
                <% end %>

            <% else %>
                <p class="error"> You have two projects already, you cannot have more > 2 projects </p>
                <% @feedback.each do |feedback| %>
                    <tr>
                      <td class="col1"><%= link_to feedback.name, "/feedbacks/#{feedback.id}" %></td>
                      <!-- <td class="col2"><%#= feedback.questions %></td>  -->
                      <td class="col2"><%= feedback.updated_at %></td>
                      <td class="col3"><%= feedback.duedate %></a></td>
                      <td class="clo4"><%= feedback.url %></td>
                      <td class="clo5"><%= feedback.avai_tester %></td>
                      <td class="clo6"><%= feedback.issues.count %></td>
                      <% if feedback.assignments.where('tester_id = ?', @tester.id).empty? %>
                          <td class="clo7 inactive"><%= link_to 'Assigned ->', {:action => :assigned, :assigned => feedback.id}, :method => :put  %></td>
                          <td class="clo8">You need to apply for this project first</td>
                      <% else %>
                          <td class="clo7">You are already Assigned</td>
                          <td class="clo8"><%= link_to 'New Issue ->', new_issue_path(:feedback_id => feedback.id) %></td>
                      <% end %>

                    </tr>

                <% end %>

            <% end %>

            </tbody>
          </table>
        </div>
      </div>
    </section>
  </div>


  <div class="span3">
    <h1>
      Welcome, <%= @tester.fname %> <br/>
    </h1>
    <%= @tester.email %> <br/>
    <%= link_to "PROJECTS", projects_path, class: "profile-btn" %>

    <ul class="nav nav-list">
      <li class="nav-header">List header</li>
      <!--<li class="active"><a href="#">Home</a></li>-->
      <li class="<%= 'active' if current_page?(projects_path) %>"><%= link_to "PROJECTS", projects_path %></li>
      <!--<li><a href="#">Library</a></li>-->
      <!--<li><a href="#">Applications</a></li>-->
      <li class="nav-header">Another list header</li>
      <li class="<%= 'active' if current_page?(current_tester) %>"><%= link_to "DASHBOARD", current_tester %></li>
      <!--<li><a href="#">Profile</a></li>-->
      <!--<li><a href="#">Settings</a></li>-->
      <!--<li class="divider"></li>-->
      <!--<li><a href="#">Help</a></li>-->
    </ul>

  </div>



</div>



